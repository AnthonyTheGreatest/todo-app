import React from "react";
import Form from "./Form";
import List from "./List";

const MainApp = () => {
  return (
    <div className="flex">
      <Form />
      <List />
    </div>
  );
};

export default MainApp;
